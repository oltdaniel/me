<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.8.6">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2020-02-09T20:33:39+01:00</updated><id>http://localhost:4000/feed.xml</id><subtitle>personal page</subtitle><entry><title type="html">Performance first</title><link href="http://localhost:4000/2020/performance-first.html" rel="alternate" type="text/html" title="Performance first" /><published>2020-03-01T00:00:00+01:00</published><updated>2020-03-01T00:00:00+01:00</updated><id>http://localhost:4000/2020/performance-first</id><content type="html" xml:base="http://localhost:4000/2020/performance-first.html">&lt;p&gt;Analyzing the traffic a website receives, is a key part in marketing to
understand and highlight the group of consumers and their interests. However,
the way how it has been implemented in the recent years has evolved from
counting single requests to tracking the use across multiple websites.&lt;/p&gt;

&lt;!--abstract--&gt;</content><author><name>Daniel Oltmanns</name></author><category term="performance," /><category term="design" /><summary type="html">Analyzing the traffic a website receives, is a key part in marketing to understand and highlight the group of consumers and their interests. However, the way how it has been implemented in the recent years has evolved from counting single requests to tracking the use across multiple websites.</summary></entry><entry><title type="html">Counting instead of tracking</title><link href="http://localhost:4000/2019/counting-instead-of-tracking.html" rel="alternate" type="text/html" title="Counting instead of tracking" /><published>2019-08-13T00:00:00+02:00</published><updated>2019-08-13T00:00:00+02:00</updated><id>http://localhost:4000/2019/counting-instead-of-tracking</id><content type="html" xml:base="http://localhost:4000/2019/counting-instead-of-tracking.html">&lt;p&gt;Analyzing the traffic a website receives, is a key part in marketing to
understand and highlight the group of consumers and their interests. However,
the way how it has been implemented in the recent years has evolved from
counting single requests to tracking the use across multiple websites.&lt;/p&gt;

&lt;!--abstract--&gt;

&lt;p&gt;We at &lt;u&gt;fcused.at&lt;/u&gt; decided against this trend and went back to the old
classics of website analysis. We start counting the unique requests, instead of
tracking the user across our website. You may ask yourself now, what the
motivation behind this solution is. First, we hate cookie banners. Every site
we will request, will have an huge cookie banner explaining how our data will
be used on the current site we are viewing. Secondly, we love minimalistic
solutions that will solve our problem in just a view lines of code.&lt;/p&gt;

&lt;p&gt;Therefore, we will follow certain rules in our solution in order to deliver a
minimalistic, and privacy protective solution to the user devices with a low
bandwidth usage. How we will receive the visitors data is limited to a simple
resource loading procedure by the browser, which means, we will not use
JavaScript in order to collect and send the data that is required on the server
side. Additionally, our solution is not allowed to store data on the client
device, e.g. cookies, as we do not want to have any kind of cookie banner on our
website.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;No JavaScript is allowed, the data needs to be sent by the browser itself.&lt;/li&gt;
  &lt;li&gt;As of 1., we are not able to extract any extra information from the client
device and are limited to the HTTP request only.&lt;/li&gt;
  &lt;li&gt;A low bandwidth will be automatically achieved, as we do not need to send
any return body, nor is the server required to send any scripts to the client
that are responsible for the data collection and sending procedure.&lt;/li&gt;
&lt;/ol&gt;

&lt;h3 id=&quot;meta-data&quot;&gt;Meta-data&lt;/h3&gt;

&lt;p&gt;The way of making the request is now done. But we need to decided on the
information we will extract from the HTTP headers or can be extracted from the
raw TCP request behind the HTTP protocol. This can be classified as meta-data.
As we specified in the goal before, we do not want to store that kind of data as
raw data on our server. However, we need some visitor specific data, that we can
use in order to identify a unique user.&lt;/p&gt;

&lt;p&gt;Storing an random unique id on the user device as an cookie would have many
advantages at this point, as it will allow us to be completely independent from
the user meta-data and count unique page views as long as the cookie is not
expired. However, we hate cookie banners, so on this kind of solution violates
the rule of our solution.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;u&gt;Referer&lt;/u&gt;: The Referer HTTP header will be delivered with the root url
the user requested, of which the received request on our server is a subordinate
request.&lt;/li&gt;
  &lt;li&gt;&lt;u&gt;IP address&lt;/u&gt;: Each request will be started by a visitors device having an
IP address. We will use this, in order to identify the users device with an
unique value.&lt;/li&gt;
  &lt;li&gt;&lt;u&gt;User-Agent&lt;/u&gt;: Multiple browsers are available on the market with
different versions and browser engines. As this setting is not totally unique,
it needs to be combined with another information.&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;storing-data&quot;&gt;Storing data&lt;/h3&gt;

&lt;p&gt;Having specified the data we are able to extract from the HTTP request, we need
to combine this data, in order to be privacy protective and unique. Another
aspect we need to add at this point, is the fact we do not want to store these
informations in raw format on our server.&lt;/p&gt;

&lt;p&gt;Lets start with the privacy protective part, that we want in our solution. The
data that is privacy relevant in the meta-data we have collect within the HTTP
request is the IP address. A way of partly anonymizing it is removing the last
part of the IP address, e.g. &lt;code&gt;1.1.1.1&lt;/code&gt; will become &lt;code&gt;1.1.1&lt;/code&gt; - see
&lt;a href=&quot;#references&quot;&gt;Reference #1&lt;/a&gt;. This procedure is also known as IP masking. This
will merge 256 different IP addresses to the same id, but 256 of 2^32 isn’t that
much. However doing this will rise the privacy level.&lt;/p&gt;

&lt;p&gt;More privacy means less uniqueness in the most cases, if nothing will be stored
on the client device in order to represent an identity. This is a common problem
with using a single value as an identifier. So let us try merging multiple
values together.&lt;/p&gt;

&lt;p&gt;Looking back into the list of meta-data we have collected from the HTTP request,
we can use the &lt;code&gt;User-Agent&lt;/code&gt; header in order to rise the level of uniqueness we
slightly shrank in the last step. By merging the reaming IP address and the
user agent of the specific user, we assigned a specific browser to a specific
client. A partly unique IP address and a partly unique browser user agent give
us way more combinations, and so on, an higher level of uniqueness.&lt;/p&gt;

&lt;p&gt;So far, so good. We assigned a unique id to a specific user. However, we need
the root url, in order to identify the page that has been originally requested.
This can be easily done, as a resource request of the browser - in this case for
an image tag - adds a &lt;code&gt;Referer&lt;/code&gt; HTTP header, which contains the url of the page
that has been originally visited. As we have only a single domain, we can reduce
this url to the path only, instead of the full url. We do not care about the
query a user can choose, as this will allow him to generate an infinite amount
of page views and unique request ids. Each request id belongs to one specific
path and one specific user.&lt;/p&gt;

&lt;p&gt;Adding the path to the hash allows the user to be even more anonymous, as the
server can only identify whether he has called the same url twice (matching
request ids) or not. Without the request hash, tracking across the paths is
possible and the privacy shrinks.&lt;/p&gt;

&lt;p&gt;The solution to the last step, hiding the actual information behind the unique
data, is hashing it. The id is then only reversible by brute-forcing every
possible input, which would be useless at this point, as the IP address it not
stored in full length in the unique data and who cares about the user agent.&lt;/p&gt;

&lt;p&gt;Summarized, our request id will be a hash of a partially IP address, the
user agent and the path that has been originally requested.&lt;/p&gt;

&lt;h3 id=&quot;code&quot;&gt;Code&lt;/h3&gt;

&lt;p&gt;The solution is now defined. A possible implementation can be found below
written in Ruby. Please note, that the provided solution is not production ready
and has no persistent storage. If this kind of concept should be moved to
production, it is suggested to add a database server, taking care of the unique
request ids. However, a cuckoo filter - or other filters - as this will filter
out same requests in order to lower the database server usage.&lt;/p&gt;

&lt;p&gt;If this functionality will be abused by other websites, the code will
automatically reject those requests by checking the domain of the &lt;code&gt;Referer&lt;/code&gt;
header.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&lt;b&gt;require&lt;/b&gt; '&lt;u&gt;sinatra&lt;/u&gt;'
&lt;b&gt;require&lt;/b&gt; '&lt;u&gt;cuckoo_filter&lt;/u&gt;'

&lt;b&gt;require&lt;/b&gt; '&lt;u&gt;uri&lt;/u&gt;'
&lt;b&gt;require&lt;/b&gt; '&lt;u&gt;digest&lt;/u&gt;'

set :port, 3001

&lt;muted&gt;# In order to identify unique requests&lt;/muted&gt;
filter = CuckooFilter.make(size: 10_000)
&lt;muted&gt;# Final statistics&lt;/muted&gt;
counts = Hash.new(0)

get '&lt;u&gt;/image.jpg&lt;/u&gt;' &lt;b&gt;do&lt;/b&gt;
  content_type '&lt;u&gt;image/jpg&lt;/u&gt;'
  &lt;muted&gt;# Referer must be given&lt;/muted&gt;
  &lt;b&gt;return if&lt;/b&gt; request.referer.nil?
  url = URI(request.referer)
  &lt;muted&gt;# Root referer host is required&lt;/muted&gt;
  &lt;b&gt;return unless&lt;/b&gt; url.host == '&lt;u&gt;fcused.at&lt;/u&gt;'
  &lt;muted&gt;# Remove last ip address block&lt;/muted&gt;
  ip = request.ip[0...request.ip.rindex('&lt;u&gt;.&lt;/u&gt;')]
  &lt;muted&gt;# Convert meta-data into non-meta-data identifier&lt;/muted&gt;
  hash = Digest::SHA256.hexdigest &quot;&lt;u&gt;#{ip}#{request.user_agent}#{url.path}&lt;/u&gt;&quot;
  &lt;muted&gt;# Check if request already exists&lt;/muted&gt;
  &lt;b&gt;unless&lt;/b&gt; filter.lookup hash
    &lt;muted&gt;# Add request if to filter&lt;/muted&gt;
    filter.insert hash
    &lt;muted&gt;# Increment counts&lt;/muted&gt;
    counts[url.path] += 1
  &lt;b&gt;end&lt;/b&gt;
&lt;b&gt;end&lt;/b&gt;

get '&lt;u&gt;/stats&lt;/u&gt;' &lt;b&gt;do&lt;/b&gt;
  &lt;muted&gt;# Return the current statistics&lt;/muted&gt;
  counts
&lt;b&gt;end&lt;/b&gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;In order to count specific requests on some pages, the following HTML need to be
added.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;&lt;b&gt;img&lt;/b&gt; src=&quot;&lt;u&gt;http://localhost:3001/image.jpg&lt;/u&gt;&quot; style=&quot;display: none;&quot;&amp;gt;&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This kind of implementation can be applied to any content type. The request
since can be reduced to under 100bytes, meaning there is no time and bandwidth
consumed in web analytics. An example implementation in golang can be found here
&lt;a href=&quot;https://github.com/oltdaniel/door&quot; target=&quot;_blank&quot;&gt;&lt;code&gt;https://github.com/oltdaniel/door&lt;/code&gt;&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h3&gt;

&lt;p&gt;As an final statement we can say, that the statistics we collect from the
requests and store in our database, deliver enough information to elaborate
facts we can reuse for marketing purposes. Besides, we can say, due to the low
amount of information, we will have a low bandwidth usage and deliver a
minimalistic solution. The privacy aspect we described in the beginning is
simply covered by the hashing of the user id and removing the last block of the
IP address.&lt;/p&gt;

&lt;hr /&gt;

&lt;h3 id=&quot;references&quot;&gt;References&lt;/h3&gt;

&lt;p&gt;&lt;strong&gt;We are not responsible for any damages done by any redirection of the
following links that do not belong the the host &lt;code&gt;fcused.at&lt;/code&gt;.&lt;/strong&gt;&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;a href=&quot;https://support.google.com/analytics/answer/2763052&quot; target=&quot;_blank&quot;&gt;IP Anonymization in Analytics by Google&lt;/a&gt;, &lt;code&gt;https://support.google.com/analytics/answer/276305&lt;/code&gt;, visited on 15th November 2018&lt;/li&gt;
&lt;/ol&gt;</content><author><name>Daniel Oltmanns</name></author><category term="analytics," /><category term="privacy" /><summary type="html">Analyzing the traffic a website receives, is a key part in marketing to understand and highlight the group of consumers and their interests. However, the way how it has been implemented in the recent years has evolved from counting single requests to tracking the use across multiple websites.</summary></entry></feed>